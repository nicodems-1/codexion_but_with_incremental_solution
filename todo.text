List of stuff i need to do next time:
->implement dongle cooldown ok
->detect burnout with the monitor thread ok
->counting the time a coder has compiled so that the monitor can detect a successfull run ok
->having ft_usleep checking for burnout ok
->having each function checking burnout before execution 


Tracking malloc and pthread_alloc to free all:

list of single mutexes ==
-uptdate_status
-status_lock
-lock_race
-print_lock

list of plurial mutexes ==
-coder_mutex
-dongle_mutex

list of mallocs == 
-dongles_array_struct
-coders_array_struct
-param_struct

parameters the function cleaning will need ==> 
--coders_array[]
--dongle_array[]

Order of execution in a normal cycle:
1 clean all the single mutexes
2 clean the plural mutexes
3 free(dongles)
4 free(coders)
5 free(param_struct);

->Implementing fifo
->implementing edf

I need to implement my cleaning function so if there is a malloc or pthread_init issue it will clean one time and kill the program;


NEW======

-> Making a good clean_exit garbage collector
->Attention avec last compile
->Attention avec les valeurs en int (assez grand ??)
->faire le readme.MD